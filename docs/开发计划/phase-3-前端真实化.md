# 阶段三：前端真实化

**状态**：🚧 进行中 | **预计工时**：2~3 天 | **前置**：阶段二

> **说明**：真实终端（xterm.js）、Monaco Diff Viewer、创建会话真实流程、画布/看板联动等核心功能已实现。当前主布局采用 `MiddleTabBar` + `TabContent` 而非顶部 Tab 栏，部分验收步骤（如 T3「直接在终端内输入」）需按实际 UI 重新验证。

---

## 3.1 真实终端（xterm.js）

- [ ] 安装：`pnpm add xterm xterm-addon-fit xterm-addon-web-links`（`apps/web`）
- [ ] 重写 `TerminalPanel.tsx`：接收 `terminal:data` WebSocket 事件，替换 Mock 输出
- [ ] 支持用户向终端输入（发送 `terminal:input`）
- [ ] 终端尺寸自适应（`FitAddon`）
- [ ] 保留滚动历史 buffer

> **注意**：`useRef` 保护初始化，`useEffect` 返回 `dispose()`，防止 React 18 Strict Mode 双重挂载泄露。

## 3.2 真实 Diff 视图

- [ ] 安装：`pnpm add @monaco-editor/react`（`apps/web`），动态 `import()` 懒加载
- [ ] 新建 `apps/web/src/components/diff/DiffViewer.tsx`：Monaco Diff Editor，接收 `diff:update` 事件
- [ ] TaskPanel 内嵌实时 Diff 摘要（文件列表 + 行数统计）
- [ ] 审批弹窗内嵌完整 Diff（Monaco Viewer）

## 3.3 会话创建真实流程

- [ ] CreateSessionDialog 提交后调用 `POST /sessions`
- [ ] 前端监听 `session:created` 事件，自动打开新 Tab
- [ ] Worktree 创建进度反馈（`initializing` 状态时显示 spinner）

## 3.4 画布 / 看板联动优化

- [ ] Canvas 节点位置变更同步到后端（持久化坐标）
- [ ] 看板拖拽触发真实状态流转（调用 `PATCH /sessions/:id/status`）
- [ ] 会话状态变更时画布节点颜色实时更新（WebSocket 驱动）


## 验收清单

### 3.1 xterm.js 真实终端

| #                           | 验收步骤                           | 预期结果                                                     |
| --------------------------- | ---------------------------------- | ------------------------------------------------------------ |
| T1                          | 新建会话（任意名称+任务描述）      | 会话 Tab 自动打开，标题栏出现 **旋转 spinner**（initializing） |
| T2                          | 等待约 2 秒                        | 终端出现 PowerShell 7 提示符（`PS >）`，**文字颜色正常** |
| T3                          | 在终端区域点击，输入 `ls` 然后回车 | 底部输入栏**不需要**，直接在终端内输入即可；列出 worktree 目录文件 |
| T4                          | 输入 `echo hello world`            | 终端回显 `hello world`                                       |
| T5                          | 输入 `ls C:\Windows\System32\drivers` | 大量输出时终端可上下滚动查看历史                             |
| T6                          | 拖拽会话详情面板边界改变窗口大小   | 终端自动 fit，列数行数跟随容器变化                           |
| T7                          | 点击终端右上角 🗑 清除按钮          | 终端内容清空                                                 |

---

### 3.2 Monaco Diff Viewer

| #    | 验收步骤                                                     | 预期结果                                                     |
| ---- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| D1   | 在已创建会话的 worktree 目录内创建/修改一个文件（如 `echo "test" > test.txt`），等待约 1 秒 | 会话详情右侧「Git Diff」区域从「暂无变更」变为 Monaco 代码编辑器（首次加载会显示「加载编辑器...」） |
| D2   | 查看 Diff 内容                                               | Monaco 编辑器以 `diff` 语法高亮显示（新增行绿色 `+`，删除行红色 `-`），只读不可编辑 |
| D3   | 查看 TaskPanel 左侧                                          | 「变更摘要」区块出现文件列表，每行显示 `A/M/D` 状态 + 文件路径 + `+x -x` 行数 |
| D4   | 关闭 Tab 重新打开该会话                                      | diff 内容仍然显示（来自 store）                              |

---

### 3.3 创建会话真实流程

| #    | 验收步骤                                 | 预期结果                                            |
| ---- | ---------------------------------------- | --------------------------------------------------- |
| C1   | 点击「新建会话」，填写表单后点击「创建」 | 弹窗立即关闭                                        |
| C2   | 观察画布/看板                            | 新节点**自动出现**，状态为「初始化中」（灰色圆点）  |
| C3   | 等待 worktree 创建完成                   | 节点颜色变绿，状态变为「运行中」；会话 Tab 自动激活 |
| C4   | 刷新浏览器页面                           | 所有会话从后端 `sessions:list` 事件恢复，无丢失     |

---

### 3.4 画布 / 看板联动

| #    | 验收步骤                                           | 预期结果                                                     |
| ---- | -------------------------------------------------- | ------------------------------------------------------------ |
| K1   | 在画布中拖拽一个会话节点到新位置，**刷新页面**     | 节点位置与拖拽后一致（坐标已持久化到 SQLite）                |
| K2   | 切换到看板视图，将「进行中」的卡片拖到「已完成」列 | 卡片移动；后端收到 `PATCH /sessions/:id/status`（可在服务器日志看到请求） |
| K3   | 切换回画布                                         | 对应节点颜色已更新（WebSocket `session:status` 事件驱动）    |

---

### 快速验收（仅需 5 分钟）

如果只想快速验证核心功能，按顺序执行：**T1 → T2 → T3 → D1（需手动改文件） → K1**，覆盖三个阶段最关键路径。